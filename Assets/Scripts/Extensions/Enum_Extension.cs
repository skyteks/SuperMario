using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class Enum_Extension
{
    //public static List<int> ReturnSelectedElements<T>(this T enumType) where T : System.Enum
    //{
    //    int count = System.Enum.GetValues(typeof(T)).Length;
    //    List<int> selectedElements = new List<int>(count);
    //    for (int i = 0; i < count; i++)
    //    {
    //        int layer = 1 << i;
    //        if (((int)enumType & layer) != 0)
    //        {
    //            selectedElements.Add(i);
    //        }
    //    }
    //    return selectedElements;
    //}
}
